# Linux 迭代 4：Nginx 蓝绿发布、观察窗口与回滚

> 文档日期：2026-09-25
> 文档状态：**规格已冻结**——第 11 节的三条已由用户拍板（Nginx 蓝绿 / 两个 slot 各自显式声明端口 /
> 观察窗口 + 手动回滚）。实现按 4a → 4b → 4c 三片推进，实现记录与验证记录在本文档之后追加。
> 对应路线图：[2026-09-21-linux-ops-tool-roadmap.md](./2026-09-21-linux-ops-tool-roadmap.md)（第 7 节）
> 前置迭代：[1c](./2026-09-21-iteration-1c.md)（`RuntimeAdapter` 与 unit 渲染）、
> [2d](./2026-09-21-iteration-2.md)（保留策略与 `prune`）、
> [3](./2026-09-24-iteration-3.md)（部署与回滚、release 目录、按 release 版本化的规格）

## 1. 定位与前置

迭代 3 交付的是**单槽部署**：一个应用一个 unit，`current` 符号链接指向当前 release，
换版本时**先停旧的、再起新的**——因此发布期间有一段时间服务是停着的。迭代 4 要的是
路线图第 7 节那句话：**新进程先起来并就绪，再把流量切过去，旧进程排空后才停**。

这两件事在根本上是冲突的，必须先说清楚：

| | 迭代 3（单槽） | 迭代 4（蓝绿） |
| --- | --- | --- |
| unit | 每个应用一个，`ExecStart` 指向 `releases/current` | **每个 slot 一个**（`<app>-blue.service` / `<app>-green.service`） |
| 换版本 | 停旧 → 起新（有中断） | 起新 slot → 就绪 → **切流** → 排空停旧 slot（无中断） |
| 谁在接流量 | `current` 指向谁就是谁 | `applications.serving_slot` + Nginx upstream |
| 回滚 | 把 `current` 切回去、重启 | 把 upstream 切回去、把旧 slot 重新拉起 |

**迭代 3 的语义对「没有声明 slot 的应用」原样保留**：manifest 里不写 `exec.slots` 的应用，
行为与今天完全一致（一个 unit、`current` 链接、先停后起）。蓝绿是**声明式的可选形态**，
不是把所有人拖过去。这样做的代价是两套部署语义同时存在——因此第 4 节把两者的边界
（哪些字段互斥、哪些字段含义随形态变化）写成**校验**，而不是留给文档去解释。

## 2. 范围与里程碑拆分

| 里程碑 | 内容 | 为什么能独立验证 |
| --- | --- | --- |
| **4a** | slot 进入领域与数据层：`Slot` 类型、release 状态机扩展（只加 `standby`，`active` 的含义扩成「正在接流量」）、
`releases.slot`、`applications.serving_slot`（迁移 `0010`）、manifest 的 `exec.slots` 与
`nginx` 段及严格校验、`domain` 侧的 slot 路径与端口解析 | 纯领域与数据层，单元测试就能钉死：
状态机、迁移、校验、路径派生，**不需要 systemd 与 Nginx** |
| **4b** | 蓝绿部署：`app deploy` 在蓝绿应用上走「起新 slot → 就绪 → 生成受管 include →
`nginx -t` → 原子替换 + reload → 观察窗口 → promote → 排空停旧 slot」；失败回滚
（upstream 切回、新 slot 停掉）；`app rollback` 把流量切回上一个可回滚版本 | 容器里**真 systemd + 真 Nginx**：
用 curl 打 Nginx 的对外端口，断言「切流前后拿到的是哪个版本的响应」——这与迭代 3b 用
「端口在听」证明切流同一个思路，但更强：**响应内容就是版本本身** |
| **4c** | 观察与对账：观察窗口的采样与时间线落库、`app slot` 系列命令、opsd 重启后按
**Nginx 实际配置 + systemd 实际状态** reconcile 出 `serving_slot`、文档与真机验证 | reconcile 是纯逻辑（给两份事实、
算一个结论），输入可以从真实文件与真实 `systemctl` 输出里取；容器里可以真的重启 opsd 再断言 |

## 3. 关键决策

### D1：蓝绿的形态 = **每 slot 一个 unit + 每 slot 一个端口 + Nginx 切流**

- unit：`<application>-<slot>.service`（**派生**，不允许手写，见 D8）；
- 端口：每个 slot 各声明自己的端口（用户 2026-09-25 拍板，见第 11 节）；
- release 目录：**仍然只有一棵**（`/opt/opsd/apps/<app>/releases/<release-id>`）。制品不可变、
  同一个制品两个 slot 都可能在跑（回滚时尤其如此），因此**不按 slot 各存一份**；
- 每个 slot 有自己的 `current` 指针：`/opt/opsd/apps/<app>/slots/<slot>/current` →
  `../../releases/<release-id>`。**不做成符号链接的符号链接**，而是直接指向 release 目录：
  解析链路越短，出问题时越好查。

代价（写清楚）：一个应用从单槽切成蓝绿时，unit 名会变（`<app>.service` → `<app>-blue.service`），
因此**已经用单槽部署过的应用不能直接改写 manifest 成蓝绿**——那是换部署形态，需要一次
显式的迁移（先把旧 unit `Stop` 掉并 `disable`，再按蓝绿形态 `Prepare`）。本迭代**不做**
自动迁移：迁移脚本与「存量应用怎么过去」列在未验证/后续项里。

### D2：谁在接流量 = `applications.serving_slot`；**对账以 Nginx 为准**

库里的 `serving_slot` 是**运营视图**，Nginx 的 upstream 是**线上事实**。两者可能不一致
（opsd 崩在「写完 include 但还没落库」之间、有人手工改了 Nginx、机器重启后状态丢了）。

规则：**对账以 Nginx 的实际配置为准**，因为那是真正决定请求去哪边的那个东西。因此：

- 受管 include 是**唯一**承载「现在指向哪个 slot」的线上事实（D3）；
- `serving_slot` 是它的镜像，每次切换**先改 include 再落库**（顺序不能反：先落库再改配置，
  中间崩溃会让库说 A、而流量在 B，且没有任何补偿）；
- `app slot` 命令与 reconcile 会**读 include 并与库比对**，不一致时以 include 为准并告警
  （把差异写进审计，而不是悄悄改库）。

### D3：Nginx 受管配置：只写自己的目录，且**必须验证它真的被加载**

- 受管目录：`<nginx.confDir>/frz-managed/<application>.conf`（`confDir` 可配置，默认
  `/etc/nginx`）；
- 内容：一个 `upstream` +（可选）一个 `server`（见 D6），upstream 指向 **serving slot 的端口**；
- **不改写用户主配置**。运维需要在主配置里 include 我们的目录（Ubuntu 默认的
  `include /etc/nginx/conf.d/*.conf;` 若指向该目录则天然满足）；
- 每次切流**都**执行一次检查：`nginx -T`（dump 全部生效配置）的输出里必须能找到我们的
  **受管文件被加载**的证据。**找不到就拒绝切流**并给出明确报错——「配置写对了但没生效」
  是最危险的状态：切流看起来成功了，流量却还在旧版本（或者根本没经过 Nginx）。
- **这一步在「候选文件写到位」之后做**（2026-09-26 按实测修正）：`nginx -T` 列的是它**实际
  加载到的文件**，而第一次部署时受管文件还不存在——先检查、后写文件的话，第一次部署必然
  以「没被加载」失败（容器 harness 第一次跑就撞上了这一条）。顺序改成「写候选 → 检查有没有
  被加载 → `nginx -t` → 原子替换 → reload」，其中**前三步都在 reload 之前**，任何一步失败
  都把原内容换回去，**流量一点没动**。

### D4：切流序列（每一步失败都要说清楚流量现在在哪）

1. **起新 slot**：`Prepare`（写 unit + 环境文件）+ `Start` 新 slot 的 unit，轮询**新 slot 的**
   就绪目标（复用 3b 的 `waitForReady`，超时预算取 `health.startTimeoutSeconds`）；
2. **写候选**：渲染指向新槽位的配置，原子写到受管文件（先写 `<app>.conf.tmp` 再 rename，
   与 `current` 切换同一个套路）；
3. **检查它真的被加载**：`nginx -T` 的输出里必须能找到这一份文件（见 D3）；
4. **校验**：`nginx -t -c <主配置>`。第 2–4 步全在 reload 之前——任何一步失败都把原内容
   换回去，**流量一点没动**；
5. **reload**：`nginx -s reload`（graceful：旧 worker 排空在途请求）。失败 → 把第 4 步替换
   掉的文件**换回原来那份**并再 reload 一次（如果连回滚 reload 都失败，报
   `NGINX_RELOAD_FAILED` 并要求人工介入——**不报「已回滚」**）；
6. **观察窗口**：按 `nginx.observationSeconds`（默认 30）**在 Operation 内**串行采样新槽位的
   **就绪探活**（间隔 200ms，与 3b 一致）。窗口内发现就绪转坏 → **按失败处理**（切回 upstream、
   停新槽位、报 `DEPLOY_ROLLED_BACK`）。
   > **2026-09-26 实现时删掉的一条**：规格原本还写了「`systemctl show -p NRestarts` 的增量」
   > （进程有没有在反复重启）。**没有采用**：`RuntimeAdapter` 端口上没有这个信息，而为一个
   > 诊断项去扩端口，会让每个实现都要各自解释「什么算一次重启」。实际影响很小——进程要是
   > 反复重启，就绪探活几乎必然会跟着抖，而抖动会被这一条抓住。
7. **promote**：落库 `serving_slot` 与 release 状态（`active`），旧 slot 的 release 转
   `superseded`；
8. **排空停旧 slot**：等 `nginx.drainSeconds`（默认 5，覆盖 reload 后旧 worker 处理在途请求的
   时间）再 `Stop` 旧 slot 的 unit。**只停进程，不删目录**——回滚就是把它重新拉起来。

### D5：失败语义沿用 3c 的三态，但「什么都没动」在蓝绿里要重新定义

3c 定下的规则：`DEPLOY_ROLLED_BACK` 只说一件事——**改动过线上并把它撤销了**；什么都没动时
返回原因码。蓝绿里的对应关系：

| 失败点 | 线上流量 | 结论 |
| --- | --- | --- |
| 新 slot 起不来 / 不就绪（第 1 步） | 未动 | 原因码（`RUNTIME_NOT_READY` 等），并说明「流量未受影响」 |
| 候选配置校验失败（第 3 步） | 未动 | `NGINX_CONFIG_INVALID` |
| reload 失败（第 5 步） | 动过（配置已替换） | 换回 + 再 reload 成功 → `DEPLOY_ROLLED_BACK`；换回也失败 → 原码 + 「需要人工介入，流量可能在旧 slot 或已中断」 |
| 观察窗口内退化（第 6 步） | 动过（流量已切到新 slot） | 切回旧 slot → `DEPLOY_ROLLED_BACK` |

新 slot 的进程在失败后一律 `Stop` 并撤掉它的 `current` 指针（与 3b 的
`teardownFirstDeploy` 同理：留着指针指着马上要删的目录，会让「现在跑的是哪个版本」指向
不存在的东西）。

### D6：manifest 只声明**事实**，不发明模板语法

`exec.slots.<slot>` 里写的是「这个 slot 听哪个端口、就绪怎么探、应用怎么知道自己的端口」，
都是运维本来就要决定的事：

```yaml
exec:
  argv: [bin/server]
  runUser: orders-api
  slots:
    blue:
      ports: [18081]
      readiness: {type: tcp, target: "127.0.0.1:18081"}
      environment: {ORDERS_HTTP_ADDR: "127.0.0.1:18081"}
    green:
      ports: [18082]
      readiness: {type: tcp, target: "127.0.0.1:18082"}
      environment: {ORDERS_HTTP_ADDR: "127.0.0.1:18082"}
```

- **应用怎么知道自己的端口**：用**已有的 `environment` 机制**（每个 slot 各写一份环境文件），
  变量名由运维定（`ORDERS_HTTP_ADDR`、`SERVER_PORT`、`PORT` 都行——取决于应用）。
  工具**不**发明 `FRZ_SLOT_PORT` 之类的注入变量：那会让「应用必须配合某个魔法名字」变成
  隐式契约，而这里本可以显式写清楚。代价是每个 slot 多两行——值得。
- **就绪目标按 slot 写**：端口不同，探活目标当然不同。写全比自动把 `health.readiness` 里的
  端口替换成 slot 端口要诚实（后者是「猜运维的意思」）。
- `health.readiness` 在蓝绿应用上**不再使用**（由每个 slot 自己声明）；同时给出两者是
  `MANIFEST_CONFLICT`，还是允许 `health` 只写 `startTimeoutSeconds`/`stopTimeoutSeconds`？
  **决定**：`health` 段退化为「只放两个超时」（`readiness` 由 slot 提供），
  因此在蓝绿应用上 `health.readiness` 若同时出现 → `MANIFEST_INVALID`，报错说清
  「就绪目标请写在 `exec.slots.<slot>.readiness`」。

### D7：`nginx` 段：对外入口也是**声明**出来的

```yaml
nginx:
  listen: 8080                      # 必填：对外端口（由 Nginx 监听）
  serverName: orders.example.com    # 可选：server_name
  upstreamName: orders-api          # 可选：默认按应用名派生，见下
  observationSeconds: 30            # 可选
  drainSeconds: 5                   # 可选
```

- upstream 名默认 `frz_<application>`（下划线：nginx 的 upstream 名里连字符可用但不在
  所有位置都友好，且这个前缀能把「我们生成的名字」与用户自己的区分开）；
- 声明了 `nginx` 段但主机上没有可用的 nginx（二进制或 `nginx.confDir`）→ `RUNTIME_UNSUPPORTED`，
  并且**在 Prepare 阶段就失败**，而不是切流时才发现；
- 没声明 `exec.slots` 却声明了 `nginx` 段 → `MANIFEST_INVALID`（Nginx 段只服务蓝绿）。

### D8：派生出来的东西不许手写

- unit 名：`<application>-<slot>.service`（蓝绿应用不得写 `systemd.unitName`）；
- `current` 指针：`/opt/opsd/apps/<app>/slots/<slot>/current`；
- 受管 Nginx 文件：`<confDir>/frz-managed/<application>.conf`；
- 环境文件：`/etc/opsd/apps/<app>.<slot>.env`（单槽应用仍是 `<app>.env`）。

理由与 1c「路径约定的唯一来源在 domain」同一条：**派生规则只能有一处**。蓝绿应用若手写了
unit 名，就会出现「工具按 slot 生成、运维按自己的名字写」两套名字，而 systemd 的 enable/
stop/status 全都要用那个名字。

## 4. manifest 增量与校验

```yaml
exec:
  ports: [18081]          # 单槽形态（与今天一致）
  slots:                  # 蓝绿形态（新增，可选）
    blue:  {ports: [...], readiness: {...}, environment: {...}}
    green: {ports: [...], readiness: {...}, environment: {...}}
nginx:                    # 蓝绿形态配套（新增，可选）
  listen: 8080
```

校验（全部 `MANIFEST_INVALID`，除注明外）：

| 规则 | 说明 |
| --- | --- |
| `exec.slots` 的键只能是 `blue` / `green`（与 `domain.Slot` 枚举同源） | 拼错即错 |
| 两个 slot **都必须给**，且 `ports` 非空 | 「只有一个 slot」不是蓝绿 |
| 两个 slot 的端口不得相交 | 相交意味着两个进程抢同一个端口，切流必然失败 |
| `exec.slots` 与 `exec.ports` 互斥 | 一个是单槽语义、一个是蓝绿语义，同时给必然是写错了 |
| `exec.slots` 与 `systemd.unitName` 互斥 | 见 D8 |
| `exec.slots` 与 `health.readiness` 互斥（`MANIFEST_INVALID`） | 见 D6 |
| 每个 slot 的 `readiness.type` 仍是 `tcp`/`http`，target 形态按 `health.readiness` 的既有规则校验 | 复用现有校验，不复制 |
| `nginx` 段必须与 `exec.slots` 同时出现；`listen` 必填且 1–65535，且不与两个 slot 的端口相交 | 对外端口与 internal 端口撞上必然出事 |
| `observationSeconds` ∈ [0, 3600]，`drainSeconds` ∈ [0, 300] | 0 表示不观察 / 不等待 |
| 蓝绿应用仍可写 `release.keepLast` | 保留策略与形态无关 |

**兼容性**：以上全是**同一 `apiVersion` 内新增的可选字段**，因此不改 `apiVersion`。
`exec.slots` 与 `nginx` 段都没出现的 manifest，解码与校验路径与迭代 3 完全一致
（新增校验只在字段出现时生效）——这一条要有回归测试钉住。

## 5. 数据表变更（迁移 `0010`）

```sql
-- releases：一次部署落在哪个 slot。NULL = 单槽形态（迭代 3 的既有语义）。
ALTER TABLE releases ADD COLUMN slot TEXT;

-- applications：现在哪一侧在接流量。NULL = 单槽形态。
-- **以 Nginx 的实际配置为准，这一列是它的镜像**（见 D2）。
ALTER TABLE applications ADD COLUMN serving_slot TEXT;

-- 每个 slot 的运营视图：它现在跑哪个 release、什么时候切的。
CREATE TABLE application_slots (
    application_id TEXT NOT NULL REFERENCES applications (id),
    slot           TEXT NOT NULL,
    release_id     TEXT,
    state          TEXT NOT NULL,          -- serving | standby | draining | stopped | failed
    switched_at    TEXT,
    updated_at     TEXT NOT NULL,
    PRIMARY KEY (application_id, slot)
);
```

release 状态机扩展（`internal/domain/release.go`）：**只加一个 `standby`**。

```
created → deploying → {standby, active, failed}
standby → {active, superseded, failed}
active → superseded
superseded → active            -- 回滚：旧的又被切回来了
任何状态 → removed             -- 保留策略清理（仍以 current / 任何 slot 指针为界）
```

- `standby`：起来了、就绪了，但**没接流量**（观察窗口内的新 slot 就是这个状态）；
- **`active` 的含义扩成「正在接流量」，不为蓝绿另立一个 `serving`**：单槽应用里
  「`current` 指向它」与蓝绿里「`serving_slot` 指向它所在的 slot」是同一件事——
  一个应用当前正在对外服务的那个版本。两个名字表达同一个概念，迟早会在某处漂移
  （`ActiveRelease`、`Rollbackable`、保留策略都要各判一次）。
  `active` 的文档注释随之改写成「正在接流量」，而不是「`current` 指向它」。
- `Rollbackable()` 仍然是 `active || superseded`，**`standby` 不进回滚目标**：
  它是一次还没成功的部署留下的半成品，回滚到它等于回滚到一个从没接过流量的版本。

**兼容性**：`ADD COLUMN` 带默认 NULL，既有行语义不变；`application_slots` 是空表起步。
迭代 3 的单槽流程**不写这两列**，因此「同一个库同时跑两种形态的应用」是允许的。

## 6. Nginx 受管配置的形态

```nginx
# 本文件由 opsd 生成，请勿手工编辑；由 <application>-<slot>.service 的部署流程维护。
# 现在指向：<slot>（release <release-id>，版本 <version>）
upstream frz_orders-api {
    server 127.0.0.1:18082;
    keepalive 16;
}

server {
    listen 8080;
    server_name orders.example.com;
    location / {
        proxy_pass http://frz_orders-api;
        proxy_http_version 1.1;
        proxy_set_header Connection "";
        proxy_set_header Host $host;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        client_max_body_size 1m;
        proxy_connect_timeout 5s;
        proxy_read_timeout 60s;
    }
}
```

**头注释里写「现在指向谁」**是刻意的：出问题时运维第一眼要看的那个事实，就在文件开头
（与 unit 头注释记录档位/版本同一个理由）。

## 7. API 与 CLI

| 命令 | 说明 |
| --- | --- |
| `opsctl app deploy` | 蓝绿应用：走第 6 节的序列。单槽应用：行为不变 |
| `opsctl app rollback [--to <version>]` | 蓝绿应用：把 upstream 切回目标版本所在的 slot（必要时先把它拉起来） |
| `opsctl app slot list --app <app>` | 两个 slot 的状态、release、端口、是否在接流量（human + `--json`） |
| `opsctl app slot history --app <app>` | 切换时间线（谁在什么时候切到哪个 release） |
| `GET /api/v1/applications/{id}/slots` | `slot list` 的 HTTP 形态 |

`app deploy` 的响应里增加 `slot`（本次落到哪个 slot）与 `switching`（是否发生了切流）。

## 8. 错误码与退出码增量

| 错误码 | HTTP | 退出码 | 含义 |
| --- | --- | --- | --- |
| `NGINX_CONFIG_INVALID` | 409 | 31 | 候选配置没通过 `nginx -t`，或受管文件没有被主配置加载 → **拒绝切流，流量未动** |
| `NGINX_RELOAD_FAILED` | 409 | 32 | reload 失败且换回原配置也没成功 → 流量状态不确定，需要人工介入 |

两者都**不进** 1d 的重试白名单：配置问题重试多少次还是配置问题。

## 9. 兼容性与迁移影响

- **manifest**：新增可选字段，`apiVersion` 不变；没有 slot 的 manifest 行为不变（有回归测试）。
- **数据表**：迁移 `0010` 只有 `ADD COLUMN` 与一张新表，既有数据语义不变。
- **错误码**：新增两个（第 8 节），已登记进 `api/v1` 的映射表与本文档。
- **已有单槽应用切蓝绿**：会换 unit 名与指针布局，**本迭代不提供自动迁移**，
  列为未验证/后续项（第 10 节）。
- **`make verify-linux` 的断言数**：4b 起容器镜像要装 nginx（Dockerfile 增一行），
  断言数会增长——以脚本运行时打印的数字为准。

## 10. 验收标准

1. 蓝绿应用的 manifest 通过严格校验；把 `exec.slots` 与 `exec.ports`、`systemd.unitName`、
   `health.readiness` 同时写会被 `MANIFEST_INVALID` 拒绝（含「端口相交」那条）。
2. **发布期间服务不中断**：容器里用 curl 以固定间隔打 Nginx 的对外端口，整个过程（含切流）
   **零失败请求**，且响应里的版本号从旧版切到新版**【Linux 容器】**。
3. 新 slot 起不来 / 不就绪时：流量**一直**在旧 slot，Operation 报原因码且说明流量未受影响。
4. `nginx -t` 失败时拒绝切流；受管文件没被主配置加载时同样拒绝（并给出可操作的报错）。
5. reload 失败时换回原配置并报告 `DEPLOY_ROLLED_BACK`；换回也失败时报
   「需要人工介入」且**不**报已回滚。
6. 观察窗口内新版本退化 → 自动切回旧 slot、报 `DEPLOY_ROLLED_BACK`、新 slot 被停且不留
   `current` 指针。
7. `app rollback` 能把流量切回上一个可回滚版本，且**连配置一起回滚**（沿用 3b 的按 release 版本化）。
8. opsd 重启后 `app slot list` 报出的 `serving_slot` 与 Nginx 实际配置一致（reconcile）。
9. 单槽应用（未声明 slots）的既有行为与断言全部不变：`make verify-linux` 原有断言回归通过。
10. `make ci` / `make verify-linux` 全绿；真机上（legacy 档 CentOS 7，若 nginx 可装）跑一轮。

## 11. 已冻结的决定（2026-09-25 用户拍板）

1. **切流机制 = Nginx 蓝绿**（按路线图第 7 节）。另两个选项「先不做蓝绿只做观察与回滚」
   与「两套按应用可选」都被否掉——后者尤其会造成「没声明时走哪条」的模糊地带。
2. **两个 slot 各自显式声明端口**（不做基准 + 偏移的魔数，也不做环境变量注入）。
3. **观察窗口 + 手动回滚**。自动回滚（窗口内超阈值自动切回）**本迭代不做**：
   真正的「错误率」要读 Nginx 日志、要定阈值，而阈值定错的自动切流比不切更危险。
   窗口内只采样**确定性的事实**（就绪是否还通过、进程是否在反复重启），发现退化按失败处理。

## 12. 未验证内容（规格阶段就写明）

- **已有单槽应用迁移到蓝绿**：unit 名与指针布局都变，本迭代不做自动迁移，也没有手工迁移的
  演练记录。
- **自动回滚**：见第 11 节第 3 条，不做。
- **错误率 / 5xx 峰值**：路线图第 7 节的验收标准里提到「以压测和故障注入验证发布期间无预期
  5xx 峰值」。本迭代能证明的是「切流期间零失败请求」（容器里用 curl 打），**不是**压测结论；
  压测与故障注入记为未验证。
- **多主机 / Nginx 与应用不在同一台机器**：属迭代 5，本迭代假设 Nginx 与 opsd 同机。
- **Nginx 之外的入口（HAProxy、云 LB）**：不做。

## 13. 4a 实现记录（2026-09-25）

范围见第 2 节：slot 进入领域与数据层。**不碰 systemd、不碰 Nginx、不碰部署流程**——
这一片要能在没有 systemd 的机器上用单元测试钉死。

### 逐文件要点

**领域（`internal/domain/slot.go`，新增）**
- `Slot`（`blue`/`green`）、`Slots`（**顺序固定的数组**，不是 map：遍历顺序不该随 map 漂移）、
  `ParseSlot`（只忽略大小写与空白，不做「猜一个最接近的」）；
- `SpecSlot`（端口 / 就绪 / 槽位自己的环境）、`SpecNginx`（对外端口、serverName、upstreamName、
  观察窗口、排空时间）、`Configured()`（`Listen > 0`）；
- **派生出来的路径与名字**：`SlotDir` / `SlotCurrentDir` / `SlotCurrentLinkTarget` /
  `SlotEnvFilePath` / `SlotSecretsEnvFilePath` / `SlotSecretsDir` / `SlotUnitName` /
  `UpstreamName` / `ManagedNginxDir` / `ManagedNginxFile`——与 1c 起就有的
  `ReleaseDir` / `EnvFilePath` / `UnitPath` 同一条纪律：**派生规则只能有一处**；
- `ApplicationSpec` 上的便捷方法：`BlueGreen` / `SlotsFor` / `SlotOf` / `SlotReadiness` /
  `SlotPorts` / `SlotUnitName` / `SlotEnvironment`（**合并** `exec.environment` 与槽位环境，
  同名时槽位优先，且只在这里合并一次）；
- 校验：`validateSlots`（两个槽位必须都给、键合法、端口非空且不越界且**不得相交**、
  槽位就绪合法、槽位环境名合法且不与 `secretEnvironment` 撞键）与 `SpecNginx.validate`
  （对外端口与槽位端口不得相交、serverName / upstreamName 的字符集、观察窗口与排空时间的范围）；
- `validateReadiness` **从 `SpecHealth.validate` 里抽出来**，槽位与单槽共用一份：
  复制一份规则出来，漂移的表现会是「单槽能过、蓝绿过不了」这种解释不清的差异。

**领域（`internal/domain/appspec.go`）**
- `SpecExec` 加 `Slots`、`ApplicationSpec` 加 `Nginx`（都是新增可选字段，`apiVersion` 不变）；
- `validateBlueGreen()` 放在**跨段**的位置：unit 名在 `Systemd` 段、就绪在 `Health` 段、
  单槽端口在 `Exec` 段，而它们与槽位的关系只有在这里才同时看得见。它负责四条互斥：
  `slots` ↔ `ports`、`slots` ↔ `systemd.unitName`、`slots` ↔ `health.readiness`、
  以及「蓝绿必须有 nginx 段 / 有 nginx 段必须是蓝绿」；
- `SpecHealth.validate(blueGreen bool)`：蓝绿应用**跳过**共享 readiness 的校验——
  那个字段在蓝绿里本就该为空，对一份「本该为空」的字段提要求只会让人以为要填两份。

**领域（`internal/domain/release.go`）**
- `ReleaseStatus` 只加一个 `ReleaseStandby`（起来了、就绪了、**没接流量**）；
- `SlotState`（`serving`/`standby`/`draining`/`stopped`/`failed`）与 `ApplicationSlot`：
  它是**槽位**的状态，与 release 状态是两个层次——同一个槽位先后跑过很多 release，
  而槽位状态只有这五种；
- `Release` 加 `Slot` 字段（**空 = 单槽形态**）。

**数据层（`migrations/0010_slots.sql`、`internal/adapters/sqlite/slot.go`）**
- 两处 `ADD COLUMN`（`releases.slot`、`applications.serving_slot`，NULL = 单槽形态）+
  `application_slots` 表（复合主键，空表起步）；
- `SetReleaseSlot` / `PutApplicationSlot` / `ApplicationSlots` / `SetServingSlot` / `ServingSlot`；
- `releaseColumns` 这个共享列清单加上 `COALESCE(slot, '')`，`scanRelease` 同步——
  列清单集中一处就是为了让「加了一处、忘了另一处」不可能发生；
- `PutApplicationSlot` 的 upsert 直接以复合主键为冲突目标：**这张表的两个主键列都是
  NOT NULL**，因此 `ON CONFLICT` 一定触发（与 `application_specs` 那次不同，见 0009 的注释）；
- `switched_at` 用 `COALESCE(excluded.switched_at, application_slots.switched_at)`：
  传 nil 表示「这次动作没切流量」，**保留**原来那个时间，而不是抹成 NULL。

**manifest（`internal/adapters/manifest/manifest.go`）**
- `wireExec.Slots` / `wireSlot` / `wireNginx`（观察窗口与排空时间用**指针**）；
- `convertSlots`（槽位名原样转过去，合法性交给 domain 的校验——一处规则一处报错）、
  `convertNginx`（默认值在**这一层**补，见下）。

### 对规格的补充与偏离

1. **`active` 的含义扩成「正在接流量」，不为蓝绿另立 `serving`**（规格原本写了 `serving`）。
   单槽的「`current` 指向它」与蓝绿的「`serving_slot` 所在槽位跑着它」是同一个概念；
   两个名字会让 `ActiveRelease` / `Rollbackable` / 保留策略三处都要各判一次，进而漂移。
   规格第 5 节已就地改写。
2. **nginx 的默认值在 manifest 层补**（`convertNginx`），不在 `domain.applyDefaults` 里。
   理由：`observationSeconds: 0` 是**显式不观察**，而「没写」要走默认的 30 秒——
   domain 的 `int` 分不出这两件事，在 `applyDefaults` 里补默认值会让「关掉观察」永远做不到。
   默认值本身仍取自 domain 的常量（同一个数字只有一处定义）。
3. **槽位环境与 `exec.environment` 合并且局部覆盖全局**：一个槽位要改的只有端口那一项，
   其余（`GOMEMLIMIT` 之类）与另一个槽位相同，让运维抄两遍没有意义。与
   `secretEnvironment` 撞键仍然报 `MANIFEST_CONFLICT`（与既有的 env/secret 撞键规则同源）。

### 发现

1. **「显式 0」与「没写」的区别，必须在能表达它的那一层处理。** 我最初把观察窗口的默认值
   放在 `applyDefaults` 里，于是 `observationSeconds: 0` 会被改写成 30——**一个合法且有
   意义的取值（不观察）被默认值吃掉了**。修法不是在 domain 加一个 `bool`，而是把默认值挪到
   wire 那一层：那里本来就有指针，本来就能分清。
2. **「测试没跑」看起来与「测试通过」几乎一样。** 我把一个 Go 测试文件写到了错误的路径
   （少了仓库上层目录），`go test -run 'BlueGreen|...'` 的输出是
   `ok ... [no tests to run]`——**退出码 0、绿色的 ok**。发现它靠的是去看 `-v` 的用例列表，
   而不是靠命令成不成功。与「MySQL 里 `||` 让指纹永远是 1」是同一类问题的两个面：
   **断言/用例根本没执行，比执行了失败更危险。**
3. **python 的字符串替换没匹配上时是静默成功的。** 给 `releaseColumns` 加列那一步，
   我按记忆写的旧文本与文件里的实际内容不一致（文件里是 `COALESCE(directory, '')` 那一版），
   替换什么都没做——直到 `scanRelease` 的 Scan 参数个数不匹配才以「expected 13 destination
   arguments in Scan, not 14」暴露出来。此后所有脚本替换都带 `assert count == 1`。

## 14. 4a 验证记录（2026-09-25）

| 命令 | 结果 | 证据类型 |
| --- | --- | --- |
| `make fmt` / `make vet` | 通过 | 静态检查 |
| `go test ./internal/domain/` | 通过（既有用例全绿，含 3c 的 `InReleaseTree` 与资源校验） | 单元测试 |
| `go test ./internal/adapters/manifest/` | 通过：新增 4 条用例——蓝绿 manifest 的完整解码（槽位端口/就绪/环境合并、nginx 默认值、upstream 名派生）、**显式 `observationSeconds: 0` 保持 0**、16 条拒绝用例（互斥四条、缺槽位、槽位名拼错、端口相交/越界/为空、槽位缺就绪、nginx 与槽位端口相撞、serverName/upstreamName 非法字符、观察窗口越界、槽位环境撞键/名非法）、**单槽 manifest 的兼容性回归**（不被识别成蓝绿、不要求 Nginx） | 单元测试 |
| `go test ./internal/adapters/sqlite/` | 通过：新增 3 条用例——`releases.slot` 的往返（含「空 = NULL」与列表路径也带回 slot）、`application_slots` 的 upsert（同一槽位只有一行、**不传 switchedAt 时保留上一次切流时间**、非法槽位/状态被拒、不存在的应用报 `APPLICATION_NOT_FOUND`）、`applications.serving_slot` 的往返与「回到单槽 = NULL」 | 单元测试（真实 SQLite 文件 + 真迁移） |
| `make ci` | 通过 | 静态 + 单元 + 集成 + e2e |

### 仍未验证（4a 的边界）

- **一切涉及进程、systemd 与 Nginx 的行为**：本片只到数据层与校验层，
  「起新槽位、切流、观察、排空」全部属 4b——**代码落地 ≠ 验证通过**。
- **`legacy` 档上的 Nginx**：真机（CentOS 7）能不能装 Nginx 还没试过，属 4b 的真机验证。
- **存量单槽应用迁移到蓝绿**：第 12 节已写明不做，也没有手工迁移的演练记录。

## 15. 4b 实现记录（一）：端口按槽位、共享命令执行、Nginx 适配器（2026-09-26）

4b 是这一轮的大头（蓝绿部署与切流）。它先要动**端口的形状**：一个应用从「一个 unit」变成
「两个槽位各一个 unit」，而「这次操作打在哪一侧」必须成为操作身份的一部分。这一段就把
这件事做完（切流流程本身在下一段）。

### 逐文件要点

**共享命令执行（`internal/adapters/execcmd`，新增）**
- `Result` / `Runner` / `Command` / `Must` 从 `runtime/systemd` 抽出来。抽的理由是它有了第二个
  消费者（nginx 适配器要跑 `nginx -t` / `nginx -s reload`），而两边各写一份最容易漂移的正是
  三件**有语义**的细节：只接受 argv、永不经过 shell；命令「根本没跑起来」才返回 err
  （退出码非零是正常结果，调用方读 ExitCode）；stderr 要留着报错用。
- `systemd/runner.go` 变成共享实现的**类型别名**（`type Runner = execcmd.Runner`）：留别名而不是
  让调用方直接写包名，是因为这个包里的用法（含测试夹具）都以「systemd 适配器怎么执行命令」
  为语境，改名只会制造无意义的 diff。

**端口的形状（`internal/application/ports.go`）**
- `RuntimeAdapter` 的 `Prepare`/`Start`/`Stop`/`Health`/`Status` 都多了一个 `slot domain.Slot`
  参数，**空槽位 = 单槽形态**（迭代 3 的语义）。`Validate` 刻意**不带**槽位：它校验的是
  「这份规格能不能跑」，而两个槽位的规格差异（端口、就绪目标）在同一个循环里一起看才完整。
- 为什么槽位是**参数**而不是塞进规格：规格对两个槽位是同一份（端口与就绪由槽位自己声明），
  把它塞进规格会造出一个「这份规格是哪个槽位的」可以写错的状态——而写错的表现是
  「操作了另一侧，且看起来成功了」。规格只有**按槽位的视图**（见 domain 的 `*For` 系列）。

**领域（`internal/domain/slot.go`、`appspec.go`）**
- 新增 `EnvFilePathFor` / `SecretsEnvFilePathFor` / `SecretsDirFor` / `SecretFileVarValueFor`：
  单槽传空槽位就得到 1c 起就在用的那些路径，蓝绿传槽位就得到该槽位自己的文件。unit 渲染、
  环境文件写入、凭据落盘、断言四处都走它们——每处各自判断一次「是不是蓝绿」，迟早会出现
  「unit 指向槽位 A 的文件、写入写到槽位 B」。
- `UnitNameFor` / `ReadinessFor` 同理；`ResolveArgv` 多一个槽位参数（蓝绿的相对 argv[0] 解析到
  该槽位的 `current`——两个 unit 指向同一个版本正是「槽位」这个词要避免的事）。
- `InSlotTree`：与 `InReleaseTree` 是同一条分工的两半。槽位目录本身由运行时适配器建
  （unit 的 `ReadWritePaths=` 可能指着它），而它**里面**的 `current` 指针归 ReleaseAdapter。
  少了这条，`exec.workingDirectory` 写成槽位的 `current` 时会被 Prepare 建成实体目录，
  紧接着的符号链接切换以「改名失败」收场（迭代 3c 在 releases 树里踩过同一个坑）。

**unit 渲染（`runtime/unitfile`）**
- `RenderUnit(spec, slot, tier, version)`：unit 名、环境文件、argv 解析都按槽位；蓝绿的
  `ReadWritePaths=` 多一条槽位目录；`Description` 写成「<app>（槽位 green）」——两个 unit
  在 `systemctl list-units` 里同名的话，出问题时第一眼看不出是哪一侧。

**两个适配器（`runtime/systemd`、`runtime/proc`）**
- systemd：`unitName := spec.UnitNameFor(slot)`、按槽位的环境/凭据路径、按槽位的就绪目标、
  `ensureDirectories` 建槽位目录并跳过两个子树的内部。
- proc：**按槽位分沙箱目录**（`root/<app>/<slot>`）。state.json 与 pid 文件按目录定位，
  两个槽位共用一份的话，第二个槽位 Prepare 时会覆盖第一个的运行时状态，而 Start/Stop
  读到的就是另一侧的记录。
- 两条都改完时，共享合约测试（`runtimecontract`）里的夹具一律传空槽位——单槽语义必须与
  迭代 3 完全一致，这一点由那套合约与既有的 1c/3b/3c 断言共同钉住。

**运行时操作的槽位（`api/v1`、`application`、`httpapi`、`cmd/opsctl`）**
- `CheckSlot(spec, raw)`：**单槽应用不能给槽位，蓝绿应用必须给**。这条校验是必需的：蓝绿应用
  的 unit 是 `<app>-<slot>.service`，少了它，一次 `runtime start` 会去打一个不存在的 unit，
  报出来的是「尚未 Prepare：unit 文件不存在」——与真实原因（少给了槽位）毫无关系。
- `v1.RuntimeOpSpec{Slot}`：槽位随 Operation 存下来。它**不是规格的一部分**（规格对两个槽位
  是同一份），执行时也推不出来，因此必须随操作存——而应用的规格仍然在执行时现读
  （与 `spec put` 的语义一致，重试拿到的是最新 manifest）。
- `--slot` 加在 `opsctl runtime prepare/health/start/stop` 上；HTTP 侧 POST 走请求体的
  `slot` 字段、health 走 `?slot=`。

### 发现

1. **「槽位」与「规格」是两个层次，混在一起会造出一个可以写错的状态。** 我最初想让适配器从
   规格里推断槽位（给规格加一个「当前槽位」字段），但那样一来：同一个规格对象被两个槽位共用
   时，「它是哪个槽位的」就成了一个随时可能不一致的字段——而它的错误表现是**静默地操作了
   另一侧**。改成端口参数之后，`Start(ctx, spec, green)` 里那个 green 是调用方必须写出来的。
2. **一处漏改会让「单槽」悄悄变成「蓝绿」。** `InSlotTree` 的缺失不会报错，只会让
   `Prepare` 把槽位的 `current` 建成实体目录——与 3c 在 releases 树里那个 bug 是同一个形状。
   这条判断与 `InReleaseTree` 并列写在一处，就是为了让下次有人加第三个子树时能看见。
3. **机械替换必须在每一步都验证。** 这一段的改动遍布二十多个调用点，我的脚本里凡是
   `assert count == 1` 的地方都挡住了「模式没匹配上却静默通过」；而**没有**加断言的那几次
   （`RenderUnit(...)` 的多行调用）就是靠编译器一个一个报出来的。两类保护都要有：
   脚本管「有没有改到」，编译器管「改得对不对」。

### 验证

| 命令 | 结果 | 证据类型 |
| --- | --- | --- |
| `make fmt` / `make vet` | 通过 | 静态检查 |
| `make ci` | 通过（44 个包全绿） | 静态 + 单元 + 集成 + e2e |

**这一段刻意不改变任何外部行为**：没有槽位的 manifest、`runtime.*` 不带 `--slot`、单槽部署
与回滚，全部与迭代 3 一致——`make ci` 里的既有断言（含 3b 的部署编排与 3c 的解释器预检）
就是这条的判据。**蓝绿切流本身还没有实现**：`exec.slots` 的 manifest 现在能通过校验、
数据层能落库，但 `app deploy` 走蓝绿分支（起新槽位 → 切流 → 观察 → 排空停旧）在下一段。

## 16. 4b 实现记录（二）：Nginx 适配器、release 适配器按槽位（2026-09-26）

上一段把端口的形状改完了；这一段补上切流流程要用到的两个适配器，并修掉一个**会让每次蓝绿
部署都失败**的校验 bug。切流流程本身（`app deploy` 的蓝绿分支）在下一段。

### 逐文件要点

**`internal/adapters/nginx`（新增）**
- `render.go`：`RenderManaged(spec, target, releaseID, version)` 与 `ParseManaged` 是纯函数，
  因此「upstream 指向哪个端口」这件事在没有 nginx 的机器上也能逐字断言；渲染与解析必须
  互为逆运算（写成一对往返用例）。
  - upstream 指向该槽位的**第一个**端口：多个端口是允许的（管理端口、指标端口），但
    upstream 只能指一个，把「对外流量的那一个」定成第一个比让工具去猜要诚实。
  - 不需要任何转义：进入正文的每个值都在 domain 层按字符集校验过（upstream 名、serverName
    各有规则，端口是整数）——与 unit 渲染要显式 `checkRenderablePath` 的情形不同。
- `nginx.go`：`Validate` / `CheckLoaded` / `Apply` / `Current`。
  - 每条命令都走共享的 `execcmd`；`nginx -v` 探测、`nginx -T` 判「有没有被加载」、
    `nginx -t` 校验、`nginx -s reload` 切流。
  - **`CheckLoaded` 是这一层最要紧的断言**：`-T` 的输出把生效配置连同每个文件的名字 dump
    出来，找不到我们的受管文件就说明主配置没有 include 它——此时切流看起来会成功，而流量
    根本不经过我们改的 upstream。报错给出可操作的下一步（`include <dir>/*.conf;`）。
  - `Apply` 的失败分支按 3c 的三态落地：校验失败 → 换回原内容且**不 reload**（流量未动）；
    reload 失败但换回成功 → `NGINX_RELOAD_FAILED` 且说明「流量仍在原来的槽位」；
    换回之后再次 reload 也失败 → `NGINX_RELOAD_FAILED` 且说明「流量状态不确定」，
    **不报「已回到旧版本」**。

**错误码**：新增 `NGINX_CONFIG_INVALID`（HTTP 409 / 退出码 31）与 `NGINX_RELOAD_FAILED`
（409 / 32），已在规格第 8 节登记。

**`ReleaseAdapter` 按槽位（`ports.go` + `release/local`）**
- `Activate` / `Deactivate` 多一个 `slot` 参数：空 = 单槽的 `releases/current`，非空 = 该槽位的
  `<app>/slots/<slot>/current`（目标是相对路径 `../../releases/<id>`，与单槽同一条理由：
  整棵树可以被整体搬走）。
- `CurrentIn(spec, slot)`：蓝绿的每一侧都有自己的答案（「这一侧现在跑的是哪个版本」）。
- `Remove` 的保底扩到**每个槽位**：删掉任何一侧正指向的目录，那一侧的下一次重启就会起不来，
  而另一侧还好好跑着——故障看起来会很随机。

### 对规格的补充与偏离

1. **`Apply` 的顺序是「先放文件、再校验」**（规格 D4 写的是「候选 → 校验 → 原子替换」）。
   原因是 nginx 的 `-t` 只能校验**已安装**的配置树，没有「校验一个候选文件」的写法；
   而 nginx 只在 reload 时读配置，因此文件换到位的那一瞬间对流量没有任何影响——真正决定
   流量的是最后一次 reload。校验失败时换回原内容（同样不 reload），流量一点没动。
2. **受管文件的头注释只记槽位**，不记 release 与版本（规格 D7 里写了三者）。理由：槽位是
   这份文件承载的那个事实，而 release/版本在库里、`app slot list` 会回答——在文件里多写
   一遍只会多一处会过期的副本。
3. **`applyDefaults` 不再给蓝绿规格补 `systemd.unitName`**（见「发现 1」）。

### 发现

1. **`applyDefaults` 补出来的默认值会让第二次校验绊倒自己。** 蓝绿规格的「不得手写 unit 名」
   是一条互斥规则，而 `applyDefaults` 会把 `UnitName` 补成 `<app>.service` 写回规格；
   于是**同一份规格第二次校验**时，它带着上一次自己补的默认值撞上了自己的规则。
   生产路径正是两次校验：`PrepareDeploy` 校验一次并把（已被默认值改写的）规格存进库，
   执行时读回来再校验一次——**每一次蓝绿部署都会在执行阶段以「不得手写 systemd.unitName」
   失败，而那份 manifest 里根本没写过它**。修法是蓝绿规格不补这个默认值（它按槽位派生），
   并加了一条「连续校验两次都要通过」的回归用例。
   这条是**测试夹具逼出来的**：我按单槽习惯写夹具、连着被校验拒绝了四次（就绪目标、nginx 段、
   unit 名、单槽 current），第四次才意识到那不是夹具的问题而是校验的问题。
2. **「构建通过」不等于「文件写对了」。** 我把 `nginx.go` 写到了少一层目录的路径上，而
   `go build ./...` 仍然通过——因为那个包里只剩 `render.go`，一个人也能编译。真正暴露它的是
   随后一条 python 脚本「文件不存在」以及 `ls`。**一个包只要还有别的文件，"少了文件但编译通过"
   就是可能的**；打错路径这类错误不会自己冒出来。（同一天我在这件事上栽了三次，此后改用
   **相对路径**写文件——Write/Edit 都接受相对 cwd 的路径，而 cwd 就是仓库根。）

### 验证

| 命令 | 结果 | 证据类型 |
| --- | --- | --- |
| `make fmt` / `make vet` | 通过 | 静态检查 |
| `go test ./internal/adapters/nginx/` | 通过：渲染与解析的往返、非蓝绿/未知槽位被拒、`-T` 缺受管文件时报 `NGINX_CONFIG_INVALID` 且给出 include 那一行、**切流命令序列是 T → t → s reload**、校验失败换回且不 reload、首次部署校验失败不留文件、reload 失败换回并说明「流量仍在原处」、两次 reload 都失败时说明「不确定」且不报已回滚 | 单元测试 |
| `go test ./internal/adapters/release/local/` | 通过：两个槽位的指针互不影响（相对目标、解析到各自的 release、**不建单槽 current**）、`CurrentIn` 按槽位回答、`Remove` 拒绝任何槽位正使用的 release | 单元测试 |
| `go test ./internal/adapters/manifest/`、`./internal/domain/` | 通过（含新增的「蓝绿规格连续校验两次都要通过」回归用例） | 单元测试 |
| `make ci` | 通过 | 静态 + 单元 + 集成 + e2e |

**仍未实现**：切流流程本身——`app deploy` 的蓝绿分支（起新槽位 → 就绪 → Nginx 切流 →
观察窗口 → promote → 排空停旧槽位）、`app slot` 系列命令与 reconcile（4c）、
以及容器里的真 Nginx 验证（那需要镜像里装 nginx 并用 curl 断言「切流前后拿到哪个版本」）。

## 17. 4b 实现记录（三）：切流流程（2026-09-26）

这一段落的是迭代 4 的正题：**蓝绿的部署与回滚**（`internal/application/deploy_slot.go`）。

### 流程

`applySlot` 是蓝绿的部署与回滚**共用的同一条路**（回滚只是「目标 release 是旧的那个」）：

```
1. 校验：Nginx 适配器在场 + 这台主机上有可用的 Nginx
2. 定目标槽位：总是**非 serving 的那一侧**；第一次部署（还没有 serving）用 blue
3. 起新槽位：Prepare（写这一侧自己的 unit 与环境）→ 物化（若需要）→ Activate（这一侧的指针）
   → Start → 等这一侧就绪            ← **旧槽位一直没动，还在接流量**
4. 切流：nginx.Apply(target)（改 upstream + `nginx -t` + reload）
5. 观察窗口：按 observationSeconds 采样新槽位的就绪，退化就按失败处理
6. promote：ActivateRelease（旧的转 superseded）→ SetServingSlot → 槽位行 serving/draining
7. 排空：等 drainSeconds 之后 Stop 旧槽位（**只停进程，不删目录**——回滚就是把它拉起来）
8. 保留策略：pruneReleases（与单槽共用）
```

### 失败语义（迭代 4 规格 D5）

| 失败点 | 流量 | 结论 |
| --- | --- | --- |
| 起新槽位 / 等就绪 | 未动 | 原因码 + 「**线上流量未受影响**（仍由槽位 X 服务）」，并停掉新槽位、把这一侧记成 `failed` |
| `nginx -t` 没通过 | 未动 | `NGINX_CONFIG_INVALID` + 「流量未受影响」 |
| reload 失败且换不回来 | **不确定** | `NGINX_RELOAD_FAILED` + 「**服务状态不确定**，需要人工确认流量在哪一侧」——**不说「流量未受影响」** |
| 观察窗口内退化 | 动过 | 切回旧槽位（`nginx.Apply(旧规格, serving)` + 确保旧槽位在跑 + 等它就绪）→ `DEPLOY_ROLLED_BACK` |
| 切回也失败 | 不确定 | 原因码 + 「切回旧槽位也失败了，当前的服务状态不确定，需要人工介入」 |

其中「reload 失败」那一行是写这一段时补的：部署流程只看到「`nginx.Apply` 报错了」，而
**配置没通过校验**与**reload 失败且换不回来**对流量意味着完全不同的事。因此失败信息按
**错误码**分流（`CodeNginxReloadFailed` → 说「不确定」；其余 → 说「未受影响」）。

### 发现

1. **「停不掉旧槽位」不该让一次成功的发布变成失败。** 排空之后停旧槽位失败（进程赖着不走、
   unit 被改过）时，流量已经在新槽位上了，旧进程只是多占一点资源。这里只记 `warn` 日志，
   发布结果保持成功——把成功的发布报成失败，比多留一个空闲进程糟得多。
2. **第一次部署失败于「切流之后」时没有旧版本可回退。** 那时唯一的诚实做法是停掉这一侧、
   撤掉对外入口，并明确报告「**这个应用现在没有任何版本在服务**」——而不是含糊地说
   「已回滚」。这条与 3b 的 `teardownFirstDeploy` 是同一个思路。
3. **`PrepareReporter` 也要按槽位**：它按 unit 名查「这次 Prepare 的档位决策」，而蓝绿的两个
   槽位各有一个 unit 名——不按槽位取，蓝绿部署的操作日志里会**没有档位记录**（查询落空、
   静默省略）。这个缺口是顺着端口的槽位参数一起补掉的。

### 验证

| 命令 | 结果 | 证据类型 |
| --- | --- | --- |
| `go test ./internal/application/ -run BlueGreen` | 通过：7 条用例——第一次部署落 blue（且**一次 Stop 都没有**）、第二次切到 green 并在**最后**停掉 blue、新槽位没就绪时**切流记录里一次都没有**（流量未动）且新槽位被停并记 `failed`、`nginx -t` 失败时报 `NGINX_CONFIG_INVALID` 且说「流量未受影响」、reload 失败时报「服务状态不确定」而**不说**未受影响、观察窗口内退化时切回旧槽位并报 `DEPLOY_ROLLED_BACK`、回滚把流量切回上一版所在的槽位并停掉原 serving 槽位 | 集成测试 |
| `make ci` | 通过 | 静态 + 单元 + 集成 + e2e |

**仍未验证**：真机（Linux 主机）上的蓝绿——容器证据见下一节。

### 容器验证（迭代 4 的真切流）

`test/linux/verify.sh` 新增 `check_bluegreen`（**27 项**）；镜像加了 `nginx` 与 `curl`。
2026-09-27 实跑 **197 项通过 / 0 项失败**（原 170 项回归不变）。这一段的证据是
**curl 拿到的正文**——它不是「端口在听」，而是「现在服务的是哪一版」：

| 断言 | 说明 |
| --- | --- |
| 主配置还没 include 受管目录时，部署**必须失败**于 `NGINX_CONFIG_INVALID`，且报错给出 include 那一行 | 「配置写对了但没生效」这一关是真的在拦 |
| 被拒绝的部署**没有让 Nginx 监听对外端口** | 拒绝是干净的：连 server 块都没生效 |
| 第一次部署后 `curl http://127.0.0.1:8080/` 得到 `version=1.0.0`，unit `frz-bg-blue` 在跑、`frz-bg-green` **没被起过**，受管配置指向 blue 的端口且**不含** green 的端口、头注释写明指向哪一侧 | |
| 第二次部署**期间**持续打请求（每 50ms 一次）：**失败数 = 0**，且成功数 ≥ 50 | 「零失败」必须配上「确实打过」才有意义 |
| 切流之后同一端口得到 `version=2.0.0`；green 在跑、**blue 被排空停掉**；两个 unit 都 enable | 这是蓝绿与「先停后起」最实质的差别 |
| `app rollback` 之后又回到 `version=1.0.0`；blue 重新在跑、green 被停 | 回滚把流量切回上一版所在的槽位 |

### 这一段暴露的三个问题（都修了）

1. **`CheckLoaded` 的顺序错了**：它问的是「受管文件有没有被主配置加载」，而**第一次部署时
   那份文件还不存在**——`nginx -T` 列的是它实际加载到的文件。容器第一次跑就以
   「受管文件没有被主配置加载」失败。修法：`Apply` 改成「写候选 → 检查有没有被加载 →
   `nginx -t` → reload」，前三步都在 reload 之前，任何一步失败都把原内容换回去、流量没动。
   规格 D3/D4 已就地改写。
2. **harness 的断言计数被丢在子 shell 里**：`first_id=$(bg_deploy ...)` 是命令替换，
   里面的 `pass`/`fail` 改的是**子 shell 的**变量——于是日志里明明有一行 `FAIL`，收尾却报
   「0 项失败」。这类失败最阴：结论是绿的，而绿是假的。修法：`bg_deploy` 改用全局变量
   `BG_OP_ID`；并且加了一道**元断言**——收尾时比对「计数」与「pass/fail 各自落的日志行数」，
   不一致就直接判失败。那道元断言能抓住所有这一类的错误，而不只是这一处。
3. **镜像里没有 `curl`**：切流全部成功（配置、unit、回滚都对），而 curl 全失败——看起来
   像产品坏了。修法：镜像加 `curl`；同时把「零失败」的断言补成「失败数 = 0 **且** 成功数
   ≥ 50」，因为「循环根本没跑起来」同样是零失败。
